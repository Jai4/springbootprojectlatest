@Library('jenkins-sharedlib') _


pipeline {
    agent {
        label 'mainmachine'
    }
    
     
     stages {
         stage('one') {
            steps {
                hello "star"
            }
        }
     }
    
   
}
